<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Product Details</title>
  <link rel="stylesheet" href="/styles/styles.css">
</head>

<body>
  <!-- Navbar -->
  <nav class="navbar">
    <a href="/" class="brand">ShopFood</a>
    <ul class="nav-links">
      <% if (user.role !=="admin" ) { %>
        <li><a href="/shopping">Shop</a></li>
        <% } %>
          <% if (user.role==="admin" ) { %>
            <li><a href="/inventory">Inventory</a></li>
            <li><a href="/addProduct">Add Product</a></li>
            <% } %>
              <li><a href="/logout">Logout</a></li>
    </ul>
  </nav>


  <!-- Product Details -->
  <div class="container product-details-page">
    <h2 class="page-title">Product Details</h2>

    <% if (product) { %>
      <div class="card">
        <p><b>Name:</b>
          <%= product.productName %>
        </p>
        <p><b>Quantity:</b>
          <%= product.quantity %>
        </p>
        <p><b>Price:</b> $<%= product.price.toFixed(2) %>
        </p>
        <p><b>Image:</b></p>
        <img src="/images/<%= product.image %>" class="product-img">
        <p><b>Details:</b>
          <%= product.details || "They are guranteed to be fresh." %>
        </p>
      </div>
      <% } else { %>
        <p class="text-center">No product found.</p>
        <% } %>

          <div class="back-link">
            <% if (user.role==="admin" ) { %>
              <a href="/inventory">Back to Inventory</a>
              <% } else { %>
                <a href="/shopping">Back to Shopping</a>
                <% } %>
          </div>
  </div>

</body>

</html>